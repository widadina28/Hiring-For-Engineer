<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ros.hiringapkforengineer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_hiengineer"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_hiengineer_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HiringApkForEngineer"
        android:usesCleartextTraffic="true">
        <activity android:name=".profile.edit.EditProfileActivity"></activity>
        <activity android:name=".profile.experience.post.AddExperienceFragment" />
        <activity android:name=".profile.portofolio.post.AddPortfolioFragment" />
        <activity android:name=".form.skill.FormSkillActivity" />
        <activity android:name=".form.FormEngineerActivity" />
        <activity android:name=".confirm.choose_status.ChooseStatusActivity" />
        <activity android:name=".home.detail.DetailEngineerActivity" />
        <activity android:name=".bottomnav.BottomNavActivity" />
        <activity android:name=".register.RegisterActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".Onboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>